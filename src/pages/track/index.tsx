import { type NextPage } from 'next';
import DefaultLayout from '~/layouts/DefaultLayout';

const Track: NextPage = () => {
	return (
		<DefaultLayout title='Track | Finance Planner'>
			<h1> Track </h1>
		</DefaultLayout>
	);
};

export default Track;
